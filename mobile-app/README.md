# Kids Tasks Mobile (Expo + Firebase)

Starter Expo (TypeScript) app for children's tasks with Firebase Firestore and Authentication.

Replace the Firebase config in src/firebaseConfig.ts with your project's values.

Commands:
- npm install
- npm run start
- npm run test
- npx eas build --platform android
